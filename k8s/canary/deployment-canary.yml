apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-ge-canary
  namespace: app
  labels:
    app: app-ge
    version: canary
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-ge
      version: canary
  template:
    metadata:
      labels:
        app: app-ge
        version: canary
    spec:
      serviceAccountName: app-ge
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      containers:
        - name: app-ge
          image: julianareiza/app-ge:canary
          ports:
            - containerPort: 8000
              protocol: TCP
          env:
            - name: APP_VERSION
              value: "canary"
            - name: APP_COMMIT
              value: "unknown"
          envFrom:
            - configMapRef:
                name: app-ge-otel-config
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /ready
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          securityContext:
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
